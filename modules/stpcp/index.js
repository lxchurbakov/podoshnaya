// Simple T(CP) Process Communication Protocol

const server = (onconnect) => {
    // take tcp server
    // forward on connect
    // return emit/on? 
    // emit to broadcast
};

const client = (url) => {
    // return emit/on
};

module.exports = { server, client };
