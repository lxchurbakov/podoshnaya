const vm = require('../vm');
// TODO use stpcp

const push = (name, filename) => {
    // todo origin
    // connect and push stuff
};

const start = (name) => {
    // todo origin
};

const stop = (name) => {
    // todo origin
};

const status = (name) => {
    // todo origin
};

const remove = (name) => {
    // todo origin
};

module.exports = { push, start, stop, status, remove };
